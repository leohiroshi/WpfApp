<UserControl x:Class="WpfApp.Views.ProdutosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Filtros -->
        <StackPanel Orientation="Vertical" Margin="0,0,0,8">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                <TextBlock Text="Nome:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                <TextBox Width="180" Text="{Binding FiltroNome, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0"/>

                <TextBlock Text="Código:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                <TextBox Width="150" Text="{Binding FiltroCodigo, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Valor Mín:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                <TextBox Width="100" Text="{Binding FiltroValorMin, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0"/>

                <TextBlock Text="Valor Máx:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                <TextBox Width="100" Text="{Binding FiltroValorMax, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0"/>

                <Button Content="Buscar" Command="{Binding BuscarCommand}" Margin="0,0,6,0"/>
                <Button Content="Recarregar" Command="{Binding CarregarCommand}"/>
            </StackPanel>
        </StackPanel>

        <!-- Grid de Produtos -->
        <DataGrid Grid.Row="1"
              ItemsSource="{Binding Produtos}"
              SelectedItem="{Binding ProdutoSelecionado}"
              AutoGenerateColumns="False"
              IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="60"/>
                <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" Width="*"/>
                <DataGridTextColumn Header="Código" Binding="{Binding Codigo}" Width="150"/>
                <DataGridTextColumn Header="Valor" Binding="{Binding Valor, StringFormat=C2}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Área de edição -->
        <GroupBox Grid.Row="2" Header="Edição" Margin="0,8,0,0">
            <Grid Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Nome:" VerticalAlignment="Center"/>
                <TextBox Grid.Column="1" Text="{Binding EditBuffer.Nome, UpdateSourceTrigger=PropertyChanged}" Margin="6,0,12,0"/>

                <TextBlock Grid.Column="2" Text="Código:" VerticalAlignment="Center"/>
                <TextBox Grid.Column="3" Text="{Binding EditBuffer.Codigo, UpdateSourceTrigger=PropertyChanged}" Margin="6,0,12,0"/>

                <TextBlock Grid.Column="4" Text="Valor:" VerticalAlignment="Center"/>
                <TextBox Grid.Column="5" Text="{Binding EditBuffer.Valor, UpdateSourceTrigger=PropertyChanged}" Margin="6,0,12,0"/>

                <StackPanel Grid.Column="6" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button Content="Incluir" Command="{Binding IncluirCommand}" Margin="0,0,6,0"/>
                    <Button Content="Editar" Command="{Binding EditarCommand}" Margin="0,0,6,0"/>
                    <Button Content="Salvar" Command="{Binding SalvarCommand}" Margin="0,0,6,0"/>
                    <Button Content="Excluir" Command="{Binding ExcluirCommand}"/>
                </StackPanel>
            </Grid>
        </GroupBox>
    </Grid>
</UserControl>